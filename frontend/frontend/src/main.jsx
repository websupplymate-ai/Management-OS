import React from "react";
import ReactDOM from "react-dom/client";
import ManagementOS from "./ManagementOS.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ManagementOS />
  </React.StrictMode>
);
